[build]
  base = "web-app"
  command = "pnpm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://wizallet-api.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
